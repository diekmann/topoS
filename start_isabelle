#!/bin/bash
TOPOS_HOME="$(readlink -f "$(pwd)/$(dirname "$0")")"

echo "setting TOPOS_HOME to: $TOPOS_HOME"

cd "$TOPOS_HOME"

export AFP_HOME="${TOPOS_HOME}/thy_lib/isabelle_afp"

isabelle jedit -d "${TOPOS_HOME}/thy_lib/isabelle_afp" -l "Collections" &
